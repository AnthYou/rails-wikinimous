<div class="container">
    <% @articles.each do |article| %>
      <div class="row">
        <h2><%= link_to article.title, article_path(article) %></h2>
      </div>
      <div class="row">
        <p><%= article.content %></p>
      </div>
  <% end %>
</div>
